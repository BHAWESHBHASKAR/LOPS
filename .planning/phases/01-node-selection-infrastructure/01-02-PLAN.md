---
phase: 01-node-selection-infrastructure
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - algorithms/node_selection.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User can rank all nodes by degree in descending order"
    - "User can select top-k nodes for any k value"
    - "Top-k selection uses heapq.nlargest() for O(n log k) performance"
    - "User can select top k% of nodes by percentage"
  artifacts:
    - path: "algorithms/node_selection.py"
      provides: "Node ranking and top-k selection methods"
      contains: ["rank_nodes_by_degree", "select_top_k", "select_top_k_percent"]
  key_links:
    - from: "algorithms/node_selection.py"
      to: "heapq module"
      via: "import heapq for top-k selection"
      pattern: "import heapq"
---

<objective>
Implement node ranking and selection methods

Purpose: Add methods to rank nodes by degree and select top-k nodes (both absolute count and percentage). This enables identification of high-importance nodes for contraction in Partial Contraction Hierarchies.

Output: NodeSelector with rank_nodes_by_degree(), select_top_k(), and select_top_k_percent() methods
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-node-selection-infrastructure/01-RESEARCH.md
@.planning/phases/01-node-selection-infrastructure/01-01-SUMMARY.md
@.planning/codebase/CONVENTIONS.md

@algorithms/node_selection.py
@algorithms/base.py
@algorithms/graph_gen.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add node ranking by degree method</name>
  <files>algorithms/node_selection.py</files>
  <action>
Extend NodeSelector class in algorithms/node_selection.py:

1. Add import: import heapq

2. Implement rank_nodes_by_degree() method:
   - Returns: List[Tuple[int, int]] where each tuple is (node_id, degree)
   - Sorted by degree in descending order (highest degree first)
   - Implementation:
     * Create list of (node, degree) tuples for all nodes in graph
     * Use sorted() with key=lambda x: x[1], reverse=True
     * Time complexity: O(n log n)
   - Use self.degree(node) to leverage caching
   - Return sorted list

3. Add comprehensive docstring:
   - Explain return format
   - Note time complexity O(n log n)
   - Example usage in docstring

Follow research recommendations:
- Use sorted() for full ranking (k ≈ n case)
- Leverage existing degree() method for caching
- Return typed tuples for clarity

Match codebase conventions:
- Type hints: -> List[Tuple[int, int]]
- Descriptive variable names
- Clear docstring
  </action>
  <verify>
python3 -c "
from algorithms.node_selection import NodeSelector
from algorithms.graph_gen import GraphGenerator

g = GraphGenerator.scale_free_graph(100, edges_per_node=2)
ns = NodeSelector(g)

# Test ranking
ranked = ns.rank_nodes_by_degree()
print(f'Top 5 nodes: {ranked[:5]}')
assert len(ranked) == 100, 'Should rank all nodes'
assert ranked[0][1] >= ranked[1][1], 'Should be sorted descending'

# Verify highest degree is actually highest
degrees = [ns.degree(i) for i in range(100)]
max_degree = max(degrees)
assert ranked[0][1] == max_degree, 'First node should have max degree'

print('✓ Ranking works correctly')
"
  </verify>
  <done>
rank_nodes_by_degree() returns all nodes sorted by degree (highest first). Result is a list of (node, degree) tuples. Order is verified to be correct.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add top-k selection methods (absolute count and percentage)</name>
  <files>algorithms/node_selection.py</files>
  <action>
Extend NodeSelector class in algorithms/node_selection.py:

1. Implement select_top_k(k: int) -> Set[int] method:
   - Validates: k > 0 and k <= graph.nodes
   - Raises ValueError if invalid
   - Uses heapq.nlargest(k, range(graph.nodes), key=self.degree)
   - Time complexity: O(n log k)
   - Returns: Set[int] of selected node IDs
   - Add docstring with complexity note

2. Implement select_top_k_percent(percentage: float) -> Set[int] method:
   - Validates: 0 < percentage <= 1.0
   - Raises ValueError if invalid
   - Calculates: k = max(1, int(self.graph.nodes * percentage))
     * Use max(1, ...) to avoid off-by-one (k=0 for small percentages)
   - Calls select_top_k(k) internally
   - Returns: Set[int] of selected node IDs
   - Add docstring with percentage examples (0.05 for 5%, 0.10 for 10%)

3. Add comprehensive docstrings:
   - Explain validation logic
   - Note time complexity O(n log k)
   - Provide usage examples
   - Note anti-pattern: why we use heapq.nlargest instead of sorted() + slice

Follow research recommendations:
- Use heapq.nlargest() for optimal O(n log k) when k << n
- Validate inputs aggressively (percentage bounds, k bounds)
- Use max(1, ...) to avoid k=0 edge case
- Return Set[int] for efficient membership tests

Match codebase conventions:
- Type hints on all signatures
- ValueError for invalid parameters (per base.py pattern)
- Descriptive error messages
- Clear docstrings
  </action>
  <verify>
python3 -c "
from algorithms.node_selection import NodeSelector
from algorithms.graph_gen import GraphGenerator

g = GraphGenerator.scale_free_graph(100, edges_per_node=2)
ns = NodeSelector(g)

# Test absolute k
top_10 = ns.select_top_k(10)
print(f'Top 10 nodes: {sorted(list(top_10))}')
assert len(top_10) == 10, 'Should select exactly 10 nodes'

# Verify selected nodes are actually top 10
ranked = ns.rank_nodes_by_degree()
top_10_ranked = set([n for n, d in ranked[:10]])
assert top_10 == top_10_ranked, 'Selected nodes should match top 10 ranked'

# Test percentage selection
top_5_percent = ns.select_top_k_percent(0.05)
print(f'Top 5% nodes: {len(top_5_percent)} nodes')
assert len(top_5_percent) == 5, '5% of 100 should be 5'

top_10_percent = ns.select_top_k_percent(0.10)
assert len(top_10_percent) == 10, '10% of 100 should be 10'

# Test validation
try:
    ns.select_top_k_percent(0.0)
    assert False, 'Should raise ValueError for 0%'
except ValueError:
    print('✓ Correctly validates percentage > 0')

try:
    ns.select_top_k_percent(1.5)
    assert False, 'Should raise ValueError for > 100%'
except ValueError:
    print('✓ Correctly validates percentage <= 1')

print('✓ Top-k selection works correctly')
"
  </verify>
  <done>
select_top_k() selects exactly k highest-degree nodes. select_top_k_percent() selects top k% of nodes (using percentage 0.0-1.0). Both validate inputs and return correct results. Uses heapq.nlargest() for optimal performance.
  </done>
</task>

</tasks>

<verification>
After completion, verify:
- [ ] rank_nodes_by_degree() returns all nodes sorted by degree
- [ ] select_top_k(k) returns exactly k highest-degree nodes
- [ ] select_top_k_percent(p) returns correct percentage of nodes
- [ ] Validation catches invalid inputs (k<=0, percentage<=0, percentage>1)
- [ ] Selected nodes match ranked nodes (correctness check)
- [ ] heapq.nlargest() is used (not sorted() + slice)
- [ ] Returns Set[int] for efficient membership tests
</verification>

<success_criteria>
User can rank all nodes by degree and select top-k nodes (both absolute count and percentage). Selection is efficient (O(n log k)) and correct. Input validation prevents errors.
</success_criteria>

<output>
After completion, create `.planning/phases/01-node-selection-infrastructure/01-02-SUMMARY.md`
</output>
