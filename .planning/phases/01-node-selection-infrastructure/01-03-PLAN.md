---
phase: 01-node-selection-infrastructure
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - tests/test_node_selection.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Test file exists with fixtures for all graph types"
    - "Degree computation tests pass on all graph types"
    - "Node ranking tests verify correct ordering"
    - "Edge cases (single node, empty graph) are tested"
  artifacts:
    - path: "tests/test_node_selection.py"
      provides: "Test fixtures and basic functionality tests"
      min_lines: 60
      contains: ["pytest.fixture", "test_degree", "test_ranking"]
  key_links:
    - from: "tests/test_node_selection.py"
      to: "algorithms/node_selection.py"
      via: "import NodeSelector for testing"
      pattern: "from algorithms.node_selection import NodeSelector"
    - from: "tests/test_node_selection.py"
      to: "algorithms/graph_gen.py"
      via: "import GraphGenerator for test fixtures"
      pattern: "from algorithms.graph_gen import GraphGenerator"
---

<objective>
Create test fixtures and basic functionality tests for node selection

Purpose: Set up pytest fixtures for different graph types and create tests for degree computation and node ranking. This establishes the testing foundation before adding more complex selection tests.

Output: Test file with fixtures and basic degree/ranking tests
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-node-selection-infrastructure/01-RESEARCH.md
@.planning/phases/01-node-selection-infrastructure/01-01-SUMMARY.md
@.planning/phases/01-node-selection-infrastructure/01-02-SUMMARY.md
@.planning/codebase/CONVENTIONS.md

@algorithms/node_selection.py
@algorithms/graph_gen.py
@algorithms/base.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test file with fixtures for different graph types</name>
  <files>tests/test_node_selection.py</files>
  <action>
Create new file tests/test_node_selection.py:

1. Import requirements:
   - import pytest
   - from algorithms.node_selection import NodeSelector
   - from algorithms.graph_gen import GraphGenerator

2. Create pytest fixtures for different graph types:
   - @pytest.fixture def grid_graph():
     * Returns 10x10 grid graph (100 nodes)
     * Uses GraphGenerator.grid_graph(10, 10)

   - @pytest.fixture def scale_free_graph():
     * Returns scale-free graph with 100 nodes
     * Uses GraphGenerator.scale_free_graph(100, edges_per_node=2)

   - @pytest.fixture def geometric_graph():
     * Returns geometric graph with 100 nodes
     * Uses GraphGenerator.geometric_graph(100, radius=15.0)
     * Returns (graph, positions) tuple

   - @pytest.fixture def road_network_graph():
     * Returns road-network-like graph with 100 nodes
     * Uses GraphGenerator.road_network_like(100, avg_degree=4)

3. Add docstrings to fixtures explaining what they provide

Follow research recommendations from 01-RESEARCH.md:
- Use parametrized fixtures for different graph types
- Test with multiple graph families to ensure generality
- Use consistent node count (100) across fixtures for percentage testing

Match codebase conventions:
- Follow pytest patterns from research
- Use descriptive fixture names
- Add docstrings
  </action>
  <verify>
# Verify tests can be discovered and fixtures work
python3 -m pytest tests/test_node_selection.py --collect-only

# Verify fixtures are callable
python3 -c "
import pytest
from tests.test_node_selection import *

# Check fixtures exist (pytest will discover them)
print('Fixtures defined successfully')
"
  </verify>
  <done>
tests/test_node_selection.py exists with pytest fixtures for grid, scale_free, geometric, and road_network graphs. Fixtures are discoverable by pytest.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add parametrized tests for degree computation and ranking</name>
  <files>tests/test_node_selection.py</files>
  <action>
Add test cases to tests/test_node_selection.py:

1. Test degree computation on different graphs:
   - def test_degree_computation_scale_free(scale_free_graph):
     * Create NodeSelector
     * Test degree() returns non-negative integers
     * Verify out_degree() <= degree() (total >= out)
     * Verify in_degree() <= degree()

   - def test_degree_caching(scale_free_graph):
     * Create NodeSelector
     * Call degree(node) twice
     * Verify second call returns cached value (could check _degree_cache directly)
     * Call clear_cache() and verify cache is empty

   - @pytest.mark.parametrize("graph_fixture", ["grid_graph", "scale_free_graph", "geometric_graph"])
     def test_degree_non_negative(graph_fixture, request):
     * Parametrize over all graph types
     * Verify all nodes have non-negative degree

2. Test node ranking:
   - def test_rank_nodes_by_degree(scale_free_graph):
     * Call rank_nodes_by_degree()
     * Verify length equals graph.nodes
     * Verify sorted in descending order
     * Verify first node has max degree

   - def test_ranking_completeness(scale_free_graph):
     * Verify all nodes appear exactly once in ranking
     * Use set comparison

3. Test edge cases:
   - def test_single_node_graph():
     * Create graph with 1 node
     * Verify degree() works (should be 0)
     * Verify rank_nodes_by_degree() returns [(0, 0)]

   - def test_empty_graph():
     * Create graph with 0 nodes (edge case)
     * Verify behavior is reasonable

Follow testing best practices:
- One assertion per test ideal, but related assertions OK
- Use pytest.mark.parametrize for similar tests
- Test edge cases explicitly
- Use descriptive test names

Match codebase conventions:
- snake_case test functions
- Descriptive names (test_degree_caching, not test_cache)
- Fixtures as parameters
  </action>
  <verify>
python3 -m pytest tests/test_node_selection.py::test_degree_computation_scale_free -v
python3 -m pytest tests/test_node_selection.py::test_rank_nodes_by_degree -v
python3 -m pytest tests/test_node_selection.py -k "ranking" -v

# Run all degree and ranking tests
python3 -m pytest tests/test_node_selection.py -k "degree or ranking" -v
  </verify>
  <done>
All degree computation tests pass. Ranking tests verify correct ordering and completeness. Edge cases handled properly. Parametrized tests run across multiple graph types.
  </done>
</task>

</tasks>

<verification>
After completion, verify:
- [ ] tests/test_node_selection.py exists and runs successfully
- [ ] All fixtures are discoverable by pytest
- [ ] Degree computation tests pass on all graph types
- [ ] Ranking tests verify correct ordering and completeness
- [ ] Edge cases (single node, empty graph) are tested
- [ ] Tests follow pytest best practices
</verification>

<success_criteria>
Test file exists with fixtures for all graph types. Basic degree and ranking tests pass. Foundation is ready for adding selection tests in Plan 04.
</success_criteria>

<output>
After completion, create `.planning/phases/01-node-selection-infrastructure/01-03-SUMMARY.md`
</output>
