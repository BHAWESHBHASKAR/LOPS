---
phase: 01-node-selection-infrastructure
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - tests/test_node_selection.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "NodeSelector works correctly on multiple graph types (grid, scale_free, geometric, road_network)"
    - "Parametrized tests cover different percentages (5%, 10%, 15%, 20%)"
    - "Tests verify selected nodes are actually highest-degree nodes"
    - "Tests validate edge cases (empty selection, single node, full graph)"
    - "Test coverage is >= 90% for node_selection.py"
  artifacts:
    - path: "tests/test_node_selection.py"
      provides: "Comprehensive parametrized test suite"
      min_lines: 100
      contains: ["test_", "pytest.fixture", "@pytest.mark.parametrize"]
  key_links:
    - from: "tests/test_node_selection.py"
      to: "algorithms/node_selection.py"
      via: "import NodeSelector for testing"
      pattern: "from algorithms.node_selection import NodeSelector"
    - from: "tests/test_node_selection.py"
      to: "algorithms/graph_gen.py"
      via: "import GraphGenerator for test fixtures"
      pattern: "from algorithms.graph_gen import GraphGenerator"
---

<objective>
Create comprehensive test suite for node selection functionality

Purpose: Validate NodeSelector works correctly across different graph types and edge cases. Use parametrized pytest fixtures to test multiple scenarios efficiently. Ensure 90%+ code coverage.

Output: Complete test suite with parametrized tests for all NodeSelector methods
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-node-selection-infrastructure/01-RESEARCH.md
@.planning/phases/01-node-selection-infrastructure/01-01-SUMMARY.md
@.planning/phases/01-node-selection-infrastructure/01-02-SUMMARY.md
@.planning/codebase/CONVENTIONS.md

@algorithms/node_selection.py
@algorithms/graph_gen.py
@algorithms/base.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test file with fixtures for different graph types</name>
  <files>tests/test_node_selection.py</files>
  <action>
Create new file tests/test_node_selection.py:

1. Import requirements:
   - import pytest
   - from algorithms.node_selection import NodeSelector
   - from algorithms.graph_gen import GraphGenerator

2. Create pytest fixtures for different graph types:
   - @pytest.fixture def grid_graph():
     * Returns 10x10 grid graph (100 nodes)
     * Uses GraphGenerator.grid_graph(10, 10)

   - @pytest.fixture def scale_free_graph():
     * Returns scale-free graph with 100 nodes
     * Uses GraphGenerator.scale_free_graph(100, edges_per_node=2)

   - @pytest.fixture def geometric_graph():
     * Returns geometric graph with 100 nodes
     * Uses GraphGenerator.geometric_graph(100, radius=15.0)
     * Returns (graph, positions) tuple

   - @pytest.fixture def road_network_graph():
     * Returns road-network-like graph with 100 nodes
     * Uses GraphGenerator.road_network_like(100, avg_degree=4)

3. Add docstrings to fixtures explaining what they provide

Follow research recommendations from 01-RESEARCH.md:
- Use parametrized fixtures for different graph types
- Test with multiple graph families to ensure generality
- Use consistent node count (100) across fixtures for percentage testing

Match codebase conventions:
- Follow pytest patterns from research
- Use descriptive fixture names
- Add docstrings
  </action>
  <verify>
# Verify tests can be discovered and fixtures work
python3 -m pytest tests/test_node_selection.py --collect-only

# Verify fixtures are callable
python3 -c "
import pytest
from tests.test_node_selection import *

# Check fixtures exist (pytest will discover them)
print('Fixtures defined successfully')
"
  </verify>
  <done>
tests/test_node_selection.py exists with pytest fixtures for grid, scale_free, geometric, and road_network graphs. Fixtures are discoverable by pytest.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add parametrized tests for degree computation and ranking</name>
  <files>tests/test_node_selection.py</files>
  <action>
Add test cases to tests/test_node_selection.py:

1. Test degree computation on different graphs:
   - def test_degree_computation_scale_free(scale_free_graph):
     * Create NodeSelector
     * Test degree() returns non-negative integers
     * Verify out_degree() <= degree() (total >= out)
     * Verify in_degree() <= degree()

   - def test_degree_caching(scale_free_graph):
     * Create NodeSelector
     * Call degree(node) twice
     * Verify second call returns cached value (could check _degree_cache directly)
     * Call clear_cache() and verify cache is empty

   - @pytest.mark.parametrize("graph_fixture", ["grid_graph", "scale_free_graph", "geometric_graph"])
     def test_degree_non_negative(graph_fixture, request):
     * Parametrize over all graph types
     * Verify all nodes have non-negative degree

2. Test node ranking:
   - def test_rank_nodes_by_degree(scale_free_graph):
     * Call rank_nodes_by_degree()
     * Verify length equals graph.nodes
     * Verify sorted in descending order
     * Verify first node has max degree

   - def test_ranking_completeness(scale_free_graph):
     * Verify all nodes appear exactly once in ranking
     * Use set comparison

3. Test edge cases:
   - def test_single_node_graph():
     * Create graph with 1 node
     * Verify degree() works (should be 0)
     * Verify rank_nodes_by_degree() returns [(0, 0)]

   - def test_empty_graph():
     * Create graph with 0 nodes (edge case)
     * Verify behavior is reasonable

Follow testing best practices:
- One assertion per test ideal, but related assertions OK
- Use pytest.mark.parametrize for similar tests
- Test edge cases explicitly
- Use descriptive test names

Match codebase conventions:
- snake_case test functions
- Descriptive names (test_degree_caching, not test_cache)
- Fixtures as parameters
  </action>
  <verify>
python3 -m pytest tests/test_node_selection.py::test_degree_computation_scale_free -v
python3 -m pytest tests/test_node_selection.py::test_rank_nodes_by_degree -v
python3 -m pytest tests/test_node_selection.py -k "ranking" -v

# Run all degree and ranking tests
python3 -m pytest tests/test_node_selection.py -k "degree or ranking" -v
  </verify>
  <done>
All degree computation tests pass. Ranking tests verify correct ordering and completeness. Edge cases handled properly. Parametrized tests run across multiple graph types.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add parametrized tests for top-k selection</name>
  <files>tests/test_node_selection.py</files>
  <action>
Add test cases to tests/test_node_selection.py:

1. Test select_top_k() absolute count:
   - @pytest.mark.parametrize("k,expected_count", [
       (1, 1), (10, 10), (50, 50), (100, 100)
     ])
     def test_select_top_k_count(scale_free_graph, k, expected_count):
     * Verify len(selected) == expected_count

   - def test_select_top_k_correctness(scale_free_graph):
     * Select top 10 nodes
     * Get ranking from rank_nodes_by_degree()
     * Verify selected nodes match top 10 from ranking
     * Use set comparison

   - def test_select_top_k_invalid(scale_free_graph):
     * Test k=0 raises ValueError
     * Test k=-1 raises ValueError
     * Test k=101 raises ValueError (graph has 100 nodes)

2. Test select_top_k_percent() percentage:
   - @pytest.mark.parametrize("percentage,expected_count", [
       (0.05, 5),   # 5% of 100 = 5
       (0.10, 10),  # 10% of 100 = 10
       (0.15, 15),  # 15% of 100 = 15
       (0.20, 20),  # 20% of 100 = 20
       (1.00, 100)  # 100% of 100 = 100
     ])
     def test_select_top_k_percent_count(scale_free_graph, percentage, expected_count):
     * Verify len(selected) == expected_count

   - @pytest.mark.parametrize("percentage", [0.05, 0.10, 0.15, 0.20])
     def test_select_top_k_percent_correctness(scale_free_graph, percentage):
     * Select top k% nodes
     * Verify they match top k% from ranking
     * Use set comparison

   - def test_select_top_k_percent_invalid(scale_free_graph):
     * Test percentage=0.0 raises ValueError
     * Test percentage=-0.1 raises ValueError
     * Test percentage=1.5 raises ValueError

3. Test selection across different graph types:
   - @pytest.mark.parametrize("graph_fixture", ["grid_graph", "scale_free_graph", "geometric_graph"])
     @pytest.mark.parametrize("percentage", [0.05, 0.10, 0.15, 0.20])
     def test_selection_works_on_all_graph_types(graph_fixture, percentage, request):
     * Get graph from fixture
     * Select top percentage
     * Verify count matches expected
     * Verify all selected nodes are valid (0 <= node < graph.nodes)

4. Test correctness verification (critical!):
   - def test_selected_nodes_are_highest_degree(scale_free_graph):
     * Select top 10% nodes
     * Get degrees of selected nodes
     * Get degrees of non-selected nodes
     * Verify min(selected_degrees) >= max(non_selected_degrees)
     * This proves selection is correct

Follow research recommendations:
- Parametrize over standard percentages (5%, 10%, 15%, 20%)
- Test on multiple graph types (grid, scale_free, geometric)
- Verify correctness: selected nodes actually have highest degrees
- Test edge cases (k=0, k=nodes, percentage=0, percentage=1)

Match codebase conventions:
- Use pytest.mark.parametrize for data-driven tests
- Descriptive test names
- Clear assertion messages
- Fixtures as parameters
  </action>
  <verify>
python3 -m pytest tests/test_node_selection.py -k "select_top" -v

# Run all selection tests
python3 -m pytest tests/test_node_selection.py::test_select_top_k_count -v
python3 -m pytest tests/test_node_selection.py::test_select_top_k_percent_count -v
python3 -m pytest tests/test_node_selection.py::test_selected_nodes_are_highest_degree -v

# Run all node selection tests
python3 -m pytest tests/test_node_selection.py -v
  </verify>
  <done>
All top-k selection tests pass. Parametrized tests verify correct counts across multiple percentages. Correctness tests prove selected nodes are actually highest-degree. Tests cover all graph types and edge cases.
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify test coverage and add missing tests if needed</name>
  <files>tests/test_node_selection.py</files>
  <action>
Run coverage analysis and add any missing tests:

1. Run pytest with coverage:
   ```bash
   python3 -m pytest tests/test_node_selection.py --cov=algorithms.node_selection --cov-report=term-missing
   ```

2. Review coverage report:
   - Identify any lines not covered
   - Add tests for missing code paths

3. Likely missing coverage (add if needed):
   - Test clear_cache() actually clears the cache
   - Test error messages are informative
   - Test very large graphs (stress test)
   - Test graphs with isolated nodes (degree=0)
   - Test directed vs undirected graph behavior

4. Add any missing test cases:
   - Create focused tests for uncovered lines
   - Ensure all branches in conditionals are tested
   - Test all exception paths

5. Target: >= 90% coverage

Follow best practices:
- Coverage is a tool, not a goal
- Focus on testing behavior, not hitting lines
- Add meaningful tests, not just coverage padding

Research recommendation from 01-RESEARCH.md:
- Aim for 90%+ coverage on node_selection.py
- Use pytest-cov to verify
  </action>
  <verify>
python3 -m pytest tests/test_node_selection.py --cov=algorithms.node_selection --cov-report=term-missing --cov-report=html

# Verify coverage percentage is >= 90%
# Check that no critical code paths are missing
# Review htmlcov/ directory for detailed coverage report
  </verify>
  <done>
Test coverage is >= 90% for algorithms/node_selection.py. All critical code paths are tested. Edge cases and error conditions covered. Coverage report confirms comprehensive testing.
  </done>
</task>

</tasks>

<verification>
After completion, verify:
- [ ] tests/test_node_selection.py exists and runs successfully
- [ ] All tests pass: pytest -v tests/test_node_selection.py
- [ ] Parametrized tests cover multiple graph types
- [ ] Parametrized tests cover percentages 5%, 10%, 15%, 20%
- [ ] Correctness tests verify selected nodes are highest-degree
- [ ] Edge cases tested (single node, empty selection, full selection)
- [ ] Error conditions tested (invalid k, invalid percentage)
- [ ] Coverage is >= 90%
- [ ] Tests follow pytest best practices
</verification>

<success_criteria>
Comprehensive test suite validates NodeSelector works correctly across all graph types and edge cases. Coverage >= 90%. Tests serve as documentation for expected behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/01-node-selection-infrastructure/01-03-SUMMARY.md`
</output>
